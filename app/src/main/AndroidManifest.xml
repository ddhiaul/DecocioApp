<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.decocioapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Masuk.ProfileCust"></activity>
        <activity android:name=".Masuk.DetailItem" />
        <activity android:name=".Masuk.DetailSubcate" />
        <activity android:name=".Masuk.SubCateCustomer" />
        <activity android:name=".HomeCustomer" />
        <activity android:name=".Masuk.RegisterActivity" />
        <activity android:name=".Masuk.LoginActivity" />
        <activity android:name=".Masuk.SplashScreen" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>